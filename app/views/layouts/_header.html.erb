<% if user_signed_in? %>
  <div class="container">
  <div class="row">
    <div class="col-md-12 text-right ">
      <%= current_user.email%>
      <%= link_to('Log out', destroy_user_session_path, class: "pl-3", method: :delete) %>
    </div>
  </div>
  </div>
<%# else  %>
<!--  <div class="sign-in"><%#= link_to('', new_user_session_path, class: "fa fa-sign-in sign-in") %></div>-->
<!--  <div class="toplabel"><%#= link_to('Войти', new_user_session_path, class: "toplabel") %></div>-->
<% end %>